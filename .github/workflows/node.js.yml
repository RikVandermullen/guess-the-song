name: Run Tests
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: nrwl/nx-set-shas@v3
      - run: npm ci
      
      - run: npx nx --GuessTheSong=test --parallel=3 --ci --code-coverage
      - run: npx nx --data-api=test --parallel=3 --ci --code-coverage
      - run: npx nx --GuessTheSong=build --parallel=3
      - run: npx nx --data-api=build --parallel=3
      - run: npx nx --neo4j=build --parallel=3
